/*
 * Created on 28 Jul 2015 ( Time 17:59:58 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.abm.mainet.common.master.ui.controller;

import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

import com.abm.mainet.common.constant.MainetConstants;
import com.abm.mainet.common.constant.MainetConstants.FormMode;
import com.abm.mainet.common.master.dto.Sysmodfunction;
import com.abm.mainet.common.master.service.SysmodfunctionService;
import com.abm.mainet.common.ui.controller.telosys.AbstractController;
import com.abm.mainet.common.utility.Message;
import com.abm.mainet.common.utility.MessageType;

/**
 * Spring MVC controller for 'Sysmodfunction' management.
 */
@Controller
@RequestMapping("/sysmodfunction")
public class SysmodfunctionController extends AbstractController {

    // --- Variables names ( to be used in JSP with Expression Language )
    private static final String MAIN_ENTITY_NAME = "sysmodfunction";
    private static final String MAIN_LIST_NAME = "list";

    // --- JSP pages names ( View name in the MVC model )
    private static final String JSP_FORM = "sysmodfunction/form";
    private static final String JSP_LIST = "sysmodfunction/list";

    // --- SAVE ACTION ( in the HTML form )
    private static final String SAVE_ACTION_CREATE = "/sysmodfunction/create";
    private static final String SAVE_ACTION_UPDATE = "/sysmodfunction/update";

    // --- Main entity service
    @Resource
    private SysmodfunctionService sysmodfunctionService; // Injected by Spring
    // --- Other service(s)

    // --------------------------------------------------------------------------------------
    /**
     * Default constructor
     */
    public SysmodfunctionController() {
        super(SysmodfunctionController.class, MAIN_ENTITY_NAME);
        log("SysmodfunctionController created.");
    }

    // --------------------------------------------------------------------------------------
    // Spring MVC model management
    // --------------------------------------------------------------------------------------

    /**
     * Populates the Spring MVC model with the given entity and eventually other useful data
     * @param model
     * @param sysmodfunction
     */
    private void populateModel(final Model model, final Sysmodfunction sysmodfunction, final FormMode formMode) {
        // --- Main entity
        model.addAttribute(MAIN_ENTITY_NAME, sysmodfunction);
        if (formMode == FormMode.CREATE) {
            model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
            model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE);
            // --- Other data useful in this screen in "create" mode (all fields)
        } else if (formMode == FormMode.UPDATE) {
            model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
            model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE);
            // --- Other data useful in this screen in "update" mode (only non-pk fields)
        }
    }

    // --------------------------------------------------------------------------------------
    // Request mapping
    // --------------------------------------------------------------------------------------
    /**
     * Shows a list with all the occurrences of Sysmodfunction found in the database
     * @param model Spring MVC model
     * @return
     */
    @RequestMapping()
    public String list(final Model model) {
        log("Action 'list'");
        final List<Sysmodfunction> list = sysmodfunctionService.findAll();
        model.addAttribute(MAIN_LIST_NAME, list);
        return JSP_LIST;
    }

    /**
     * Shows a form page in order to create a new Sysmodfunction
     * @param model Spring MVC model
     * @return
     */
    @RequestMapping("/form")
    public String formForCreate(final Model model) {
        log("Action 'formForCreate'");
        // --- Populates the model with a new instance
        final Sysmodfunction sysmodfunction = new Sysmodfunction();
        populateModel(model, sysmodfunction, FormMode.CREATE);
        return JSP_FORM;
    }

    /**
     * Shows a form page in order to update an existing Sysmodfunction
     * @param model Spring MVC model
     * @param smfid primary key element
     * @return
     */
    @RequestMapping(value = "/form/{smfid}")
    public String formForUpdate(final Model model, @PathVariable("smfid") final Long smfid) {
        log("Action 'formForUpdate'");
        // --- Search the entity by its primary key and stores it in the model
        final Sysmodfunction sysmodfunction = sysmodfunctionService.findById(smfid);
        populateModel(model, sysmodfunction, FormMode.UPDATE);
        return JSP_FORM;
    }

    /**
     * 'CREATE' action processing. <br>
     * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
     * @param sysmodfunction entity to be created
     * @param bindingResult Spring MVC binding result
     * @param model Spring MVC model
     * @param redirectAttributes Spring MVC redirect attributes
     * @param httpServletRequest
     * @return
     */
    @RequestMapping(value = "/create") // GET or POST
    public String create(@Valid final Sysmodfunction sysmodfunction, final BindingResult bindingResult, final Model model,
            final RedirectAttributes redirectAttributes, final HttpServletRequest httpServletRequest) {
        log("Action 'create'");
        try {
            if (!bindingResult.hasErrors()) {
                final Sysmodfunction sysmodfunctionCreated = sysmodfunctionService.create(sysmodfunction);
                model.addAttribute(MAIN_ENTITY_NAME, sysmodfunctionCreated);

                // ---
                messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS, MainetConstants.COMMON_STATUS.SAVE_OK));
                return redirectToForm(httpServletRequest, sysmodfunction.getSmfid());
            } else {
                populateModel(model, sysmodfunction, FormMode.CREATE);
                return JSP_FORM;
            }
        } catch (final Exception e) {
            log("Action 'create' : Exception - " + e.getMessage());
            messageHelper.addException(model, "sysmodfunction.error.create", e);
            populateModel(model, sysmodfunction, FormMode.CREATE);
            return JSP_FORM;
        }
    }

    /**
     * 'UPDATE' action processing. <br>
     * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
     * @param sysmodfunction entity to be updated
     * @param bindingResult Spring MVC binding result
     * @param model Spring MVC model
     * @param redirectAttributes Spring MVC redirect attributes
     * @param httpServletRequest
     * @return
     */
    @RequestMapping(value = "/update") // GET or POST
    public String update(@Valid final Sysmodfunction sysmodfunction, final BindingResult bindingResult, final Model model,
            final RedirectAttributes redirectAttributes, final HttpServletRequest httpServletRequest) {
        log("Action 'update'");
        try {
            if (!bindingResult.hasErrors()) {
                // --- Perform database operations
                final Sysmodfunction sysmodfunctionSaved = sysmodfunctionService.update(sysmodfunction);
                model.addAttribute(MAIN_ENTITY_NAME, sysmodfunctionSaved);
                // --- Set the result message
                messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS, MainetConstants.COMMON_STATUS.SAVE_OK));
                log("Action 'update' : update done - redirect");
                return redirectToForm(httpServletRequest, sysmodfunction.getSmfid());
            } else {
                log("Action 'update' : binding errors");
                populateModel(model, sysmodfunction, FormMode.UPDATE);
                return JSP_FORM;
            }
        } catch (final Exception e) {
            messageHelper.addException(model, "sysmodfunction.error.update", e);
            log("Action 'update' : Exception - " + e.getMessage());
            populateModel(model, sysmodfunction, FormMode.UPDATE);
            return JSP_FORM;
        }
    }

    /**
     * 'DELETE' action processing. <br>
     * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
     * @param redirectAttributes
     * @param smfid primary key element
     * @return
     */
    @RequestMapping(value = "/delete/{smfid}") // GET or POST
    public String delete(final RedirectAttributes redirectAttributes, @PathVariable("smfid") final Long smfid) {
        log("Action 'delete'");
        try {
            sysmodfunctionService.delete(smfid);
            // --- Set the result message
            messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS, MainetConstants.DELETE));
        } catch (final Exception e) {
            messageHelper.addException(redirectAttributes, "sysmodfunction.error.delete", e);
        }
        return redirectToList();
    }

}
