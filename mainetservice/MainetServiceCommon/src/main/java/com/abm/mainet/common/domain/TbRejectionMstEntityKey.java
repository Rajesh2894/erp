/*
 * Created on 5 Apr 2016 ( Time 11:55:07 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.abm.mainet.common.domain;

import java.io.Serializable;

import javax.persistence.Column;
import javax.persistence.Embeddable;

/**
 * Composite primary key for entity "TbRejectionMstEntity" ( stored in table "TB_REJECTION_MST" )
 *
 * @author Telosys Tools Generator
 *
 */
@Embeddable
public class TbRejectionMstEntityKey implements Serializable {

    private static final long serialVersionUID = 1L;

    // ----------------------------------------------------------------------
    // ENTITY KEY ATTRIBUTES
    // ----------------------------------------------------------------------
    @Column(name = "REJ_ID", nullable = false)
    private Long rejId;

    @Column(name = "ORGID", nullable = false)
    private Long orgid;

    // ----------------------------------------------------------------------
    // CONSTRUCTORS
    // ----------------------------------------------------------------------
    public TbRejectionMstEntityKey() {
        super();
    }

    public TbRejectionMstEntityKey(final Long rejId, final Long orgid) {
        super();
        this.rejId = rejId;
        this.orgid = orgid;
    }

    // ----------------------------------------------------------------------
    // GETTERS & SETTERS FOR KEY FIELDS
    // ----------------------------------------------------------------------
    public void setRejId(final Long value) {
        rejId = value;
    }

    public Long getRejId() {
        return rejId;
    }

    public void setOrgid(final Long value) {
        orgid = value;
    }

    public Long getOrgid() {
        return orgid;
    }

    // ----------------------------------------------------------------------
    // equals METHOD
    // ----------------------------------------------------------------------
    @Override
    public boolean equals(final Object obj) {
        if (this == obj) {
            return true;
        }
        if (obj == null) {
            return false;
        }
        if (this.getClass() != obj.getClass()) {
            return false;
        }
        final TbRejectionMstEntityKey other = (TbRejectionMstEntityKey) obj;
        // --- Attribute rejId
        if (rejId == null) {
            if (other.rejId != null) {
                return false;
            }
        } else if (!rejId.equals(other.rejId)) {
            return false;
        }
        // --- Attribute orgid
        if (orgid == null) {
            if (other.orgid != null) {
                return false;
            }
        } else if (!orgid.equals(other.orgid)) {
            return false;
        }
        return true;
    }

    // ----------------------------------------------------------------------
    // hashCode METHOD
    // ----------------------------------------------------------------------
    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;

        // --- Attribute rejId
        result = (prime * result) + ((rejId == null) ? 0 : rejId.hashCode());
        // --- Attribute orgid
        result = (prime * result) + ((orgid == null) ? 0 : orgid.hashCode());

        return result;
    }

    // ----------------------------------------------------------------------
    // toString METHOD
    // ----------------------------------------------------------------------
    @Override
    public String toString() {
        final StringBuffer sb = new StringBuffer();
        sb.append(rejId);
        sb.append("|");
        sb.append(orgid);
        return sb.toString();
    }
}
